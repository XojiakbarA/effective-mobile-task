## Run the following command in the project root directory to run the app:
### `docker compose up -d`
## Run the following command in the project root directory to stop the app:
### `docker compose down`